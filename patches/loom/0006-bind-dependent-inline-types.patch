From 212f5061dc2582ba3bdaf419ce40aac37b69795d Mon Sep 17 00:00:00 2001
From: Zach Denton <z@chdenton.com>
Date: Wed, 9 Sep 2026 11:05:45 +0200
Subject: [PATCH] [Loom] Bind dependent types before checking inline boundaries

Compare callee types after mapping argument dimensions and encoding references into the caller. Refine projected types with shared exact facts when ordinary substitution is insufficient; acquire the fact snapshot lazily once per pass and retain it for rewriting. Use the same comparison for cloned and transferred return replacements.

Cover runtime dimensions, multiple static widths, identity and computed vector returns, and template vector arguments through native source-to-low. Validation: 550 available compiler fixture suites pass. Four additional source-low suites have identical pre-existing failures when rebuilding the unchanged parent commit.
---
 loom/src/loom/rewrite/BUILD.bazel             |   1 +
 loom/src/loom/rewrite/CMakeLists.txt          |   1 +
 loom/src/loom/rewrite/callable.c              |  79 ++++++++++--
 loom/src/loom/rewrite/callable.h              |   9 ++
 .../source_low_template_expansion.loom-test   |  74 ++++++++++++
 loom/src/loom/transforms/symbol/BUILD.bazel   |   1 +
 .../src/loom/transforms/symbol/CMakeLists.txt |   1 +
 .../loom/transforms/symbol/inline_callables.c | 112 ++++++++++++++----
 .../symbol/test/inline_callables.loom-test    |  79 ++++++++++++
 9 files changed, 319 insertions(+), 38 deletions(-)

diff --git a/loom/src/loom/rewrite/BUILD.bazel b/loom/src/loom/rewrite/BUILD.bazel
index c66f85cde..195bc2145 100644
--- a/loom/src/loom/rewrite/BUILD.bazel
+++ b/loom/src/loom/rewrite/BUILD.bazel
@@ -155,6 +155,7 @@ loom_cc_library(
         ":remap",
         ":rewriter",
         "//loom/src/loom/analysis:availability",
+        "//loom/src/loom/analysis:type_refinement",
         "//loom/src/loom/ir",
         "//loom/src/loom/ops:op_defs",
         "//loom/src/loom/ops/cfg",
diff --git a/loom/src/loom/rewrite/CMakeLists.txt b/loom/src/loom/rewrite/CMakeLists.txt
index 7c4e08ab8..55f7eb1b5 100644
--- a/loom/src/loom/rewrite/CMakeLists.txt
+++ b/loom/src/loom/rewrite/CMakeLists.txt
@@ -168,6 +168,7 @@ loom_cc_library(
     "callable.c"
     "callable_outline.c"
   DEPS
+    loom::analysis::type_refinement
     ::materialize
     ::remap
     ::rewriter
diff --git a/loom/src/loom/rewrite/callable.c b/loom/src/loom/rewrite/callable.c
index 2e923c22d..7e2e6890f 100644
--- a/loom/src/loom/rewrite/callable.c
+++ b/loom/src/loom/rewrite/callable.c
@@ -6,12 +6,43 @@
 
 #include "loom/rewrite/callable.h"
 
+#include "loom/analysis/type_refinement.h"
 #include "loom/ir/context.h"
 #include "loom/ir/module.h"
 #include "loom/ops/cfg/ops.h"
 #include "loom/ops/func/ops.h"
 #include "loom/rewrite/materialize.h"
 
+iree_status_t loom_callable_type_matches(loom_ir_remap_t* remap,
+                                         const loom_value_fact_table_t* facts,
+                                         loom_type_t callee_type,
+                                         loom_type_t call_type,
+                                         bool* out_matches) {
+  if (loom_type_equal(callee_type, call_type)) {
+    *out_matches = true;
+    return iree_ok_status();
+  }
+  loom_type_t bound_type;
+  IREE_RETURN_IF_ERROR(loom_ir_remap_type(remap, callee_type, &bound_type));
+  if (facts) {
+    loom_type_refinement_result_t refinement;
+    IREE_RETURN_IF_ERROR(loom_type_refine_with_value_facts(
+        bound_type, facts, remap->arena, &bound_type, &refinement));
+    if (refinement == LOOM_TYPE_REFINEMENT_CONFLICT) {
+      *out_matches = false;
+      return iree_ok_status();
+    }
+    IREE_RETURN_IF_ERROR(loom_type_refine_with_value_facts(
+        call_type, facts, remap->arena, &call_type, &refinement));
+    if (refinement == LOOM_TYPE_REFINEMENT_CONFLICT) {
+      *out_matches = false;
+      return iree_ok_status();
+    }
+  }
+  *out_matches = loom_type_equal(bound_type, call_type);
+  return iree_ok_status();
+}
+
 static bool loom_callable_get_call_symbol_ref(const loom_module_t* module,
                                               const loom_op_t* call_op,
                                               loom_symbol_ref_t* out_ref) {
@@ -228,9 +259,10 @@ static bool loom_callable_value_is_valid(const loom_module_t* module,
 }
 
 static iree_status_t loom_callable_validate_cfg_body(
-    const loom_module_t* module, const loom_op_t* call_op,
+    loom_rewriter_t* rewriter, const loom_op_t* call_op,
     loom_func_like_t callee, loom_call_like_t call,
     loom_callable_cfg_body_t* out_body) {
+  const loom_module_t* module = rewriter->module;
   *out_body = (loom_callable_cfg_body_t){0};
   loom_region_t* body = loom_func_like_body(callee);
   if (!body || body->block_count == 0) {
@@ -268,6 +300,10 @@ static iree_status_t loom_callable_validate_cfg_body(
         IREE_STATUS_FAILED_PRECONDITION,
         "callee entry block argument count does not match its signature");
   }
+  loom_ir_remap_t remap;
+  const loom_ir_remap_options_t options = {.allow_unmapped_values = true};
+  IREE_RETURN_IF_ERROR(loom_ir_remap_initialize(
+      rewriter->module, rewriter->module, rewriter->arena, &options, &remap));
   for (uint16_t i = 0; i < argument_count; ++i) {
     if (!loom_callable_value_is_valid(module, arguments[i]) ||
         !loom_callable_value_is_valid(module, call_operands.values[i])) {
@@ -279,9 +315,16 @@ static iree_status_t loom_callable_validate_cfg_body(
           IREE_STATUS_FAILED_PRECONDITION,
           "callee entry block argument does not match its signature");
     }
-    if (!loom_type_equal(
-            loom_module_value_type(module, arguments[i]),
-            loom_module_value_type(module, call_operands.values[i]))) {
+    IREE_RETURN_IF_ERROR(loom_ir_remap_map_value(
+        &remap, arguments[i], call_operands.values[i]));
+  }
+  for (uint16_t i = 0; i < argument_count; ++i) {
+    bool matches;
+    IREE_RETURN_IF_ERROR(loom_callable_type_matches(
+        &remap, rewriter->fact_table,
+        loom_module_value_type(module, arguments[i]),
+        loom_module_value_type(module, call_operands.values[i]), &matches));
+    if (!matches) {
       return iree_make_status(
           IREE_STATUS_FAILED_PRECONDITION,
           "call operand type does not match callee argument type");
@@ -302,8 +345,12 @@ static iree_status_t loom_callable_validate_cfg_body(
       return iree_make_status(IREE_STATUS_INVALID_ARGUMENT,
                               "call and callee results must be valid");
     }
-    if (!loom_type_equal(loom_module_value_type(module, call_results.values[i]),
-                         loom_module_value_type(module, callee_results[i]))) {
+    bool matches;
+    IREE_RETURN_IF_ERROR(loom_callable_type_matches(
+        &remap, rewriter->fact_table,
+        loom_module_value_type(module, callee_results[i]),
+        loom_module_value_type(module, call_results.values[i]), &matches));
+    if (!matches) {
       return iree_make_status(
           IREE_STATUS_FAILED_PRECONDITION,
           "call result type does not match callee result type");
@@ -355,9 +402,12 @@ static iree_status_t loom_callable_validate_cfg_body(
         return iree_make_status(IREE_STATUS_INVALID_ARGUMENT,
                                 "callee return operand must be valid");
       }
-      if (!loom_type_equal(
-              loom_module_value_type(module, return_values[i]),
-              loom_module_value_type(module, call_results.values[i]))) {
+      bool matches;
+      IREE_RETURN_IF_ERROR(loom_callable_type_matches(
+          &remap, rewriter->fact_table,
+          loom_module_value_type(module, return_values[i]),
+          loom_module_value_type(module, call_results.values[i]), &matches));
+      if (!matches) {
         return iree_make_status(
             IREE_STATUS_FAILED_PRECONDITION,
             "callee return operand type does not match call result type");
@@ -419,8 +469,11 @@ static iree_status_t loom_callable_resolve_return_replacements(
     loom_type_t result_type =
         loom_module_value_type(rewriter->module, call_results_slice.values[i]);
     loom_type_t replacement_type =
-        loom_module_value_type(rewriter->module, replacements[i]);
-    if (!loom_type_equal(result_type, replacement_type)) {
+        loom_module_value_type(rewriter->module, return_operands.values[i]);
+    bool matches = false;
+    IREE_RETURN_IF_ERROR(loom_callable_type_matches(
+        remap, rewriter->fact_table, replacement_type, result_type, &matches));
+    if (!matches) {
       return iree_make_status(
           IREE_STATUS_FAILED_PRECONDITION,
           "inline replacement type does not match call result type");
@@ -799,7 +852,7 @@ iree_status_t loom_callable_inline_call_with_branch(
 
   loom_callable_cfg_body_t body = {0};
   IREE_RETURN_IF_ERROR(loom_callable_validate_cfg_body(
-      rewriter->module, call_op, callee, call, &body));
+      rewriter, call_op, callee, call, &body));
   if (loom_callable_body_is_linear(rewriter->module, callee)) {
     return loom_callable_inline_single_block_call(rewriter, call_op, callee);
   }
@@ -985,7 +1038,7 @@ iree_status_t loom_callable_inline_consuming_call_with_branch(
 
   loom_callable_cfg_body_t body = {0};
   IREE_RETURN_IF_ERROR(loom_callable_validate_cfg_body(
-      rewriter->module, call_op, callee, call, &body));
+      rewriter, call_op, callee, call, &body));
   if (!availability ||
       !loom_callable_body_is_linear(rewriter->module, callee)) {
     return loom_callable_inline_consuming_cfg_call(rewriter, call_op, callee,
diff --git a/loom/src/loom/rewrite/callable.h b/loom/src/loom/rewrite/callable.h
index dd2fa4ae1..c4ecb37bc 100644
--- a/loom/src/loom/rewrite/callable.h
+++ b/loom/src/loom/rewrite/callable.h
@@ -28,6 +28,15 @@ iree_status_t loom_callable_resolve_direct_callee(const loom_module_t* module,
                                                   const loom_op_t* call_op,
                                                   loom_func_like_t* out_callee);
 
+// Compares a callee type with a call-site type after binding SSA references.
+// Exact value facts may specialize dimensions or encoding attachments. This
+// performs no conversion and does not change either value's authored type.
+iree_status_t loom_callable_type_matches(loom_ir_remap_t* remap,
+                                         const loom_value_fact_table_t* facts,
+                                         loom_type_t callee_type,
+                                         loom_type_t call_type,
+                                         bool* out_matches);
+
 // Inlines |callee| at |call_op| using clone materialization.
 //
 // The callee must be a same-module function-like op. A body consisting of one
diff --git a/loom/src/loom/target/arch/amdgpu/test/source_low/source_low_template_expansion.loom-test b/loom/src/loom/target/arch/amdgpu/test/source_low/source_low_template_expansion.loom-test
index 5bf74bdc0..1b2b60750 100644
--- a/loom/src/loom/target/arch/amdgpu/test/source_low/source_low_template_expansion.loom-test
+++ b/loom/src/loom/target/arch/amdgpu/test/source_low/source_low_template_expansion.loom-test
@@ -89,3 +89,77 @@ low.kernel.def target<amdgpu.gfx11.generic.core>(@gfx11) workgroup_size(256, 1,
 ^_bb3:
   return
 }
+
+// ====
+
+// RUN: emit source-low output=none
+// Specialized vector arguments bind through the template boundary.
+amdgpu.target<gfx11-generic> @probe_gfx11 {subgroup_size = 32}
+
+config.def @probe.registers = 2 : index
+
+template.decl @probe.first(%width: index, %value: vector<[%width]xi32>) -> (i32)
+
+template.def<@probe.first> @first(%width: index, %value: vector<[%width]xi32>) -> (i32) {
+  %zero = index.constant 0 : index
+  %result = vector.extract %value[%zero] : vector<[%width]xi32> -> i32
+  template.return %result : i32
+}
+
+kernel.def target(@probe_gfx11) export("dynamic_argument") @dynamic_argument() {
+  %one = index.constant 1 : index
+  %threads = index.constant 32 : index
+  kernel.launch.config workgroups(%one, %one, %one) workgroup_size(%threads, %one, %one) : index
+} launch(%input: buffer, %output: buffer) {
+  %zero = index.constant 0 : index
+  %offset = index.constant 0 : offset
+  %width = config.get @probe.registers : index
+  %lane = kernel.subgroup.lane.id : index
+  %ig = buffer.assume.memory_space<global> %input : buffer
+  %og = buffer.assume.memory_space<global> %output : buffer
+  %iv = buffer.view %ig[%offset] : buffer -> view<32x4xi32>
+  %ov = buffer.view %og[%offset] : buffer -> view<32xi32>
+  %value = vector.load %iv[%lane, %zero] : view<32x4xi32> -> vector<[%width]xi32>
+  %result = template.apply<@probe.first>(%width, %value) : (index, vector<[%width]xi32>) -> (i32)
+  view.store %result, %ov[%lane] : i32, view<32xi32>
+  kernel.return
+}
+
+// ----
+
+// ====
+
+// RUN: emit source-low output=none
+// Specialized vector arguments bind through the template boundary.
+amdgpu.target<gfx11-generic> @probe_gfx11 {subgroup_size = 32}
+
+config.def @probe.registers = 4 : index
+
+template.decl @probe.first(%width: index, %value: vector<[%width]xi32>) -> (i32)
+
+template.def<@probe.first> @first(%width: index, %value: vector<[%width]xi32>) -> (i32) {
+  %zero = index.constant 0 : index
+  %result = vector.extract %value[%zero] : vector<[%width]xi32> -> i32
+  template.return %result : i32
+}
+
+kernel.def target(@probe_gfx11) export("dynamic_argument") @dynamic_argument() {
+  %one = index.constant 1 : index
+  %threads = index.constant 32 : index
+  kernel.launch.config workgroups(%one, %one, %one) workgroup_size(%threads, %one, %one) : index
+} launch(%input: buffer, %output: buffer) {
+  %zero = index.constant 0 : index
+  %offset = index.constant 0 : offset
+  %width = config.get @probe.registers : index
+  %lane = kernel.subgroup.lane.id : index
+  %ig = buffer.assume.memory_space<global> %input : buffer
+  %og = buffer.assume.memory_space<global> %output : buffer
+  %iv = buffer.view %ig[%offset] : buffer -> view<32x4xi32>
+  %ov = buffer.view %og[%offset] : buffer -> view<32xi32>
+  %value = vector.load %iv[%lane, %zero] : view<32x4xi32> -> vector<[%width]xi32>
+  %result = template.apply<@probe.first>(%width, %value) : (index, vector<[%width]xi32>) -> (i32)
+  view.store %result, %ov[%lane] : i32, view<32xi32>
+  kernel.return
+}
+
+// ----
diff --git a/loom/src/loom/transforms/symbol/BUILD.bazel b/loom/src/loom/transforms/symbol/BUILD.bazel
index 4541b2a60..113ce88ee 100644
--- a/loom/src/loom/transforms/symbol/BUILD.bazel
+++ b/loom/src/loom/transforms/symbol/BUILD.bazel
@@ -189,6 +189,7 @@ loom_cc_library(
         "//loom/src/loom/ops/low",
         "//loom/src/loom/pass:pipeline",
         "//loom/src/loom/pass:types",
+        "//loom/src/loom/pass:value_facts",
         "//loom/src/loom/rewrite:callable",
         "//loom/src/loom/rewrite:rewriter",
         "//loom/src/loom/target:function_version",
diff --git a/loom/src/loom/transforms/symbol/CMakeLists.txt b/loom/src/loom/transforms/symbol/CMakeLists.txt
index 7dfd94c49..76e7749ff 100644
--- a/loom/src/loom/transforms/symbol/CMakeLists.txt
+++ b/loom/src/loom/transforms/symbol/CMakeLists.txt
@@ -197,6 +197,7 @@ loom_cc_library(
   SRCS
     "inline_callables.c"
   DEPS
+    loom::pass::value_facts
     iree::base
     loom::analysis::availability
     loom::analysis::scc
diff --git a/loom/src/loom/transforms/symbol/inline_callables.c b/loom/src/loom/transforms/symbol/inline_callables.c
index 17f9559ba..f0a9c8686 100644
--- a/loom/src/loom/transforms/symbol/inline_callables.c
+++ b/loom/src/loom/transforms/symbol/inline_callables.c
@@ -19,6 +19,7 @@
 #include "loom/ops/low/ops.h"
 #include "loom/ops/op_defs.h"
 #include "loom/pass/pipeline.h"
+#include "loom/pass/value_facts.h"
 #include "loom/rewrite/callable.h"
 #include "loom/rewrite/rewriter.h"
 #include "loom/target/function_version.h"
@@ -252,6 +253,8 @@ typedef struct loom_inline_state_t {
   loom_module_t* module;
   // Mutable compiler-version owner, or NULL outside target compilation.
   loom_function_version_owner_t* version_owner;
+  // Shared facts for boundary type specialization, maintained during rewriting.
+  loom_value_fact_table_t* facts;
   // Reference table built from the immutable module snapshot.
   loom_symbol_reference_table_t references;
   // Target versions observed against the immutable module snapshot.
@@ -892,12 +895,6 @@ static loom_inline_blocker_t loom_inline_validate_inline_body(
         call_operands.values[i] >= module->values.count) {
       return LOOM_INLINE_BLOCKER_INVALID_OPERAND_OR_ARGUMENT;
     }
-    loom_type_t arg_type = loom_module_value_type(module, arg_ids[i]);
-    loom_type_t operand_type =
-        loom_module_value_type(module, call_operands.values[i]);
-    if (!loom_type_equal(arg_type, operand_type)) {
-      return LOOM_INLINE_BLOCKER_OPERAND_TYPE_MISMATCH;
-    }
   }
 
   loom_value_slice_t call_results = loom_call_like_results(entry->call);
@@ -911,13 +908,6 @@ static loom_inline_blocker_t loom_inline_validate_inline_body(
         call_results.values[i] >= module->values.count) {
       return LOOM_INLINE_BLOCKER_INVALID_RETURN_OR_RESULT;
     }
-    loom_type_t callee_result_type =
-        loom_module_value_type(module, callee_results[i]);
-    loom_type_t result_type =
-        loom_module_value_type(module, call_results.values[i]);
-    if (!loom_type_equal(callee_result_type, result_type)) {
-      return LOOM_INLINE_BLOCKER_RESULT_TYPE_MISMATCH;
-    }
   }
 
   for (uint16_t block_index = 0; block_index < body->block_count;
@@ -948,13 +938,6 @@ static loom_inline_blocker_t loom_inline_validate_inline_body(
       if (return_operands[i] >= module->values.count) {
         return LOOM_INLINE_BLOCKER_INVALID_RETURN_OR_RESULT;
       }
-      const loom_type_t return_type =
-          loom_module_value_type(module, return_operands[i]);
-      const loom_type_t result_type =
-          loom_module_value_type(module, call_results.values[i]);
-      if (!loom_type_equal(return_type, result_type)) {
-        return LOOM_INLINE_BLOCKER_RESULT_TYPE_MISMATCH;
-      }
     }
   }
 
@@ -966,18 +949,96 @@ static loom_inline_blocker_t loom_inline_validate_inline_body(
   return LOOM_INLINE_BLOCKER_NONE;
 }
 
-static void loom_inline_preflight_required_entries(loom_inline_state_t* state) {
+static iree_status_t loom_inline_types_match(loom_inline_state_t* state,
+                                             loom_ir_remap_t* remap,
+                                             loom_type_t source,
+                                             loom_type_t target,
+                                             bool* out_matches) {
+  IREE_RETURN_IF_ERROR(loom_callable_type_matches(remap, state->facts, source,
+                                                  target, out_matches));
+  if (*out_matches || state->facts) return iree_ok_status();
+  // Ordinary calls require only SSA substitution. Acquire the shared fact
+  // snapshot once, and only when exact dimensions or encodings are needed.
+  IREE_RETURN_IF_ERROR(loom_pass_value_facts_acquire(
+      state->pass, state->module, loom_pass_value_fact_scope_module(),
+      &state->facts));
+  return loom_callable_type_matches(remap, state->facts, source, target,
+                                    out_matches);
+}
+
+static iree_status_t loom_inline_preflight_required_entries(
+    loom_inline_state_t* state) {
   for (uint32_t i = 0; i < state->entry_count; ++i) {
     loom_inline_plan_entry_t* entry = &state->entries[i];
-    if (entry->action != LOOM_INLINE_PLAN_ACTION_REQUIRED) {
-      continue;
-    }
+    if (entry->action != LOOM_INLINE_PLAN_ACTION_REQUIRED) continue;
     loom_inline_blocker_t blocker =
         loom_inline_validate_inline_body(state, entry);
     if (blocker != LOOM_INLINE_BLOCKER_NONE) {
       loom_inline_mark_blocker(entry, blocker);
+      continue;
+    }
+    loom_ir_remap_t remap;
+    const loom_ir_remap_options_t options = {.allow_unmapped_values = true};
+    IREE_RETURN_IF_ERROR(loom_ir_remap_initialize(
+        state->module, state->module, state->pass->arena, &options, &remap));
+    uint16_t count;
+    const loom_value_id_t* args = loom_func_like_arg_ids(entry->callee, &count);
+    const loom_value_slice_t operands = loom_call_like_operands(entry->call);
+    for (uint16_t j = 0; j < count; ++j) {
+      IREE_RETURN_IF_ERROR(
+          loom_ir_remap_map_value(&remap, args[j], operands.values[j]));
+    }
+    for (uint16_t j = 0; j < count; ++j) {
+      bool matches;
+      IREE_RETURN_IF_ERROR(loom_inline_types_match(
+          state, &remap, loom_module_value_type(state->module, args[j]),
+          loom_module_value_type(state->module, operands.values[j]), &matches));
+      if (!matches) {
+        loom_inline_mark_blocker(entry,
+                                 LOOM_INLINE_BLOCKER_OPERAND_TYPE_MISMATCH);
+        break;
+      }
+    }
+    if (entry->action == LOOM_INLINE_PLAN_ACTION_ERROR) continue;
+    const loom_value_slice_t results = loom_call_like_results(entry->call);
+    const loom_value_id_t* callee_results = loom_op_const_results(entry->callee.op);
+    for (uint16_t j = 0; j < results.count; ++j) {
+      bool matches;
+      IREE_RETURN_IF_ERROR(loom_inline_types_match(
+          state, &remap,
+          loom_module_value_type(state->module, callee_results[j]),
+          loom_module_value_type(state->module, results.values[j]), &matches));
+      if (!matches) {
+        loom_inline_mark_blocker(entry, LOOM_INLINE_BLOCKER_RESULT_TYPE_MISMATCH);
+        break;
+      }
+    }
+    if (entry->action == LOOM_INLINE_PLAN_ACTION_ERROR) continue;
+    // Upstream supports CFG callees: check each return, not the entry block's
+    // terminator, which may be a branch rather than a return.
+    loom_region_t* body = loom_func_like_body(entry->callee);
+    const loom_region_descriptor_t* descriptor = loom_op_vtable_region_descriptor(
+        loom_op_vtable(state->module, entry->callee.op),
+        loom_func_like_body_region_index(entry->callee));
+    for (uint16_t block_index = 0; block_index < body->block_count; ++block_index) {
+      loom_block_t* block = loom_region_block(body, block_index);
+      const loom_op_t* ret = loom_block_op(block, block->op_count - 1);
+      if (ret->kind != descriptor->terminator) continue;
+      for (uint16_t j = 0; j < results.count; ++j) {
+        bool matches;
+        IREE_RETURN_IF_ERROR(loom_inline_types_match(
+            state, &remap,
+            loom_module_value_type(state->module, loom_op_const_operands(ret)[j]),
+            loom_module_value_type(state->module, results.values[j]), &matches));
+        if (!matches) {
+          loom_inline_mark_blocker(entry, LOOM_INLINE_BLOCKER_RESULT_TYPE_MISMATCH);
+          break;
+        }
+      }
+      if (entry->action == LOOM_INLINE_PLAN_ACTION_ERROR) break;
     }
   }
+  return iree_ok_status();
 }
 
 static iree_status_t loom_inline_emit_blockers(loom_inline_state_t* state) {
@@ -1302,6 +1363,7 @@ static iree_status_t loom_inline_execute_plan(loom_inline_state_t* state) {
   loom_rewriter_t rewriter = {0};
   IREE_RETURN_IF_ERROR(
       loom_rewriter_initialize(&rewriter, state->module, state->pass->arena));
+  rewriter.fact_table = state->facts;
 
   iree_status_t status =
       loom_inline_materialize_locked_low_schedules(state, &rewriter);
@@ -1491,7 +1553,7 @@ iree_status_t loom_inline_callables_run(loom_pass_t* pass,
   IREE_RETURN_IF_ERROR(loom_inline_index_required_entries_by_component(&state));
   loom_inline_mark_cycle_blockers(&state);
   loom_inline_propagate_required_cfg_shapes(&state);
-  loom_inline_preflight_required_entries(&state);
+  IREE_RETURN_IF_ERROR(loom_inline_preflight_required_entries(&state));
   IREE_RETURN_IF_ERROR(loom_inline_emit_blockers(&state));
   if (loom_pass_has_error_diagnostics(pass)) {
     return iree_ok_status();
diff --git a/loom/src/loom/transforms/symbol/test/inline_callables.loom-test b/loom/src/loom/transforms/symbol/test/inline_callables.loom-test
index 96666f797..f315d0923 100644
--- a/loom/src/loom/transforms/symbol/test/inline_callables.loom-test
+++ b/loom/src/loom/transforms/symbol/test/inline_callables.loom-test
@@ -876,3 +876,82 @@ func.def public @consume_loop_wrapper(%value: i32) -> (i32) {
   }
   func.return %result : i32
 }
+
+// ====
+
+// Each call binds the same dependent signature to a different static width.
+// RUN: pass canonicalize,inline-callables,canonicalize
+func.def inline @identity(%width: index, %value: vector<[%width]xi32>) -> (vector<[%width]xi32>) {
+  func.return %value : vector<[%width]xi32>
+}
+
+func.def public @two_specializations() {
+  %two = index.constant 2 : index
+  %four = index.constant 4 : index
+  %a = vector.constant 7 : vector<[%two]xi32>
+  %b = vector.constant 9 : vector<[%four]xi32>
+  %x = func.call @identity(%two, %a) : (index, vector<[%two]xi32>) -> (vector<[%two]xi32>)
+  %y = func.call @identity(%four, %b) : (index, vector<[%four]xi32>) -> (vector<[%four]xi32>)
+  test.use %x : vector<[%two]xi32>
+  test.use %y : vector<[%four]xi32>
+  func.return
+}
+
+// ----
+func.def public @two_specializations() {
+  %a = vector.constant 7 : vector<2xi32>
+  %b = vector.constant 9 : vector<4xi32>
+  test.use %a : vector<2xi32>
+  test.use %b : vector<4xi32>
+  func.return
+}
+
+// ====
+
+// Runtime dimensions still bind by SSA identity; they need not become static.
+// RUN: pass inline-callables
+func.def inline @identity(%width: index, %value: vector<[%width]xi32>) -> (vector<[%width]xi32>) {
+  func.return %value : vector<[%width]xi32>
+}
+
+func.def public @runtime_width(%n: index, %input: vector<[%n]xi32>) -> (vector<[%n]xi32>) {
+  %result = func.call @identity(%n, %input) : (index, vector<[%n]xi32>) -> (vector<[%n]xi32>)
+  func.return %result : vector<[%n]xi32>
+}
+
+// ----
+func.def public @runtime_width(%n: index, %input: vector<[%n]xi32>) -> (vector<[%n]xi32>) {
+  func.return %input : vector<[%n]xi32>
+}
+
+// ====
+
+
+// Each call binds the same dependent signature to a different static width.
+// RUN: pass canonicalize,inline-callables,canonicalize
+func.def inline @double_vector(%width: index, %value: vector<[%width]xi32>) -> (vector<[%width]xi32>) {
+  %sum = vector.addi %value, %value : vector<[%width]xi32>
+  func.return %sum : vector<[%width]xi32>
+}
+
+func.def public @two_computed_specializations() {
+  %two = index.constant 2 : index
+  %four = index.constant 4 : index
+  %a = vector.constant 7 : vector<[%two]xi32>
+  %b = vector.constant 9 : vector<[%four]xi32>
+  %x = func.call @double_vector(%two, %a) : (index, vector<[%two]xi32>) -> (vector<[%two]xi32>)
+  %y = func.call @double_vector(%four, %b) : (index, vector<[%four]xi32>) -> (vector<[%four]xi32>)
+  test.use %x : vector<[%two]xi32>
+  test.use %y : vector<[%four]xi32>
+  func.return
+}
+
+
+// ----
+func.def public @two_computed_specializations() {
+  %sum$12 = vector.constant 14 : vector<2xi32>
+  %sum$11 = vector.constant 18 : vector<4xi32>
+  test.use %sum$12 : vector<2xi32>
+  test.use %sum$11 : vector<4xi32>
+  func.return
+}
-- 
2.55.0

