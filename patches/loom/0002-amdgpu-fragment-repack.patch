From 15451788364499c23c109003314271dff4fc45b4 Mon Sep 17 00:00:00 2001
From: Zach Denton <z@chdenton.com>
Date: Wed, 9 Sep 2026 13:38:56 +0200
Subject: [PATCH] amdgpu: preserve fragment layout and refresh lowering
 fixtures

---
 .../target/arch/amdgpu/descriptors/alu.py     |  21 ++
 .../target/arch/amdgpu/descriptors/sets.py    |   2 +
 .../amdgpu/lower/matrix_fragment_repack.c     |  11 ++
 .../lower/matrix_fragment_repack_packed_b16.c | 185 ++++++++++++++++++
 .../lower/matrix_fragment_repack_packed_b16.h |  10 +
 loom/src/loom/target/arch/amdgpu/lower/plan.h |  16 ++
 .../arch/amdgpu/lower/subgroup_crosslane.c    |  60 +++++-
 .../arch/amdgpu/lower/subgroup_shuffle.c      |   3 +-
 ...w_mma_fragment_repack_packed_b16.loom-test | 126 +++++++-----
 .../source_low_subgroup_collective.loom-test  |   8 +-
 10 files changed, 383 insertions(+), 59 deletions(-)

diff --git a/loom/py/loom/target/arch/amdgpu/descriptors/alu.py b/loom/py/loom/target/arch/amdgpu/descriptors/alu.py
index a6ce1aa6f..74c047c53 100644
--- a/loom/py/loom/target/arch/amdgpu/descriptors/alu.py
+++ b/loom/py/loom/target/arch/amdgpu/descriptors/alu.py
@@ -2469,6 +2469,26 @@ def _v_permlanex16_b32_src12_inline_overlay() -> AmdgpuDescriptorOverlay:
     )
 
 
+def _v_permlanex16_b32_src12_sgpr_overlay() -> AmdgpuDescriptorOverlay:
+    """v_permlanex16_b32 with both lane selectors in SGPRs (any 32-bit value)."""
+    return AmdgpuDescriptorOverlay(
+        descriptor_key="amdgpu.v_permlanex16_b32.src12_sgpr",
+        instruction_name="V_PERMLANEX16_B32",
+        mnemonic="v_permlanex16_b32_src12_sgpr",
+        encoding_name="ENC_VOP3",
+        semantic_tag="lane.permlanex16.b32",
+        schedule_class=_SCHEDULE_VALU,
+        operands=(
+            AmdgpuOperandOverlay("VDST", _vgpr_result()),
+            AmdgpuOperandOverlay("SRC0", _vgpr_operand("src")),
+            AmdgpuOperandOverlay("SRC1", _sgpr_operand("selector_low")),
+            AmdgpuOperandOverlay("SRC2", _sgpr_operand("selector_high")),
+        ),
+        effects=(_CONVERGENT_EFFECT,),
+        flags=(DescriptorFlag.DEAD_REMOVABLE,),
+    )
+
+
 def _v_lshrrev_b32_overlay() -> AmdgpuDescriptorOverlay:
     return _v_binary_u32_overlay(
         descriptor_key="amdgpu.v_lshrrev_b32",
@@ -7065,6 +7085,7 @@ __all__ = (
     "_v_perm_b32_src2_literal_overlay",
     "_v_perm_b32_src1_zero_src2_literal_overlay",
     "_v_permlanex16_b32_src12_inline_overlay",
+    "_v_permlanex16_b32_src12_sgpr_overlay",
     "_v_lshl_add_u32_shift_immediate_overlay",
     "_v_lshl_add_u32_shift_immediate_src2_literal_overlay",
     "_v_lshlrev_b32_literal_overlay",
diff --git a/loom/py/loom/target/arch/amdgpu/descriptors/sets.py b/loom/py/loom/target/arch/amdgpu/descriptors/sets.py
index 4abe0af4b..197e159cb 100644
--- a/loom/py/loom/target/arch/amdgpu/descriptors/sets.py
+++ b/loom/py/loom/target/arch/amdgpu/descriptors/sets.py
@@ -1029,6 +1029,7 @@ def _gfx11_core_overlays() -> tuple[AmdgpuDescriptorOverlay, ...]:
         *_integer_bitwise_shift_overlays(),
         *_integer_bitwise_permute_overlays(),
         _v_permlanex16_b32_src12_inline_overlay(),
+        _v_permlanex16_b32_src12_sgpr_overlay(),
         *_v_binary_f32_overlays(),
         *_v_binary_f16_overlays(),
         _v_med3_num_f32_overlay(),
@@ -1838,6 +1839,7 @@ def _rdna4_core_overlays() -> tuple[AmdgpuDescriptorOverlay, ...]:
         *_integer_bitwise_shift_overlays(),
         *_integer_bitwise_permute_overlays(),
         _v_permlanex16_b32_src12_inline_overlay(),
+        _v_permlanex16_b32_src12_sgpr_overlay(),
         *_v_binary_f32_overlays(),
         *_v_binary_f16_overlays(),
         _v_med3_num_f32_overlay(
diff --git a/loom/src/loom/target/arch/amdgpu/lower/matrix_fragment_repack.c b/loom/src/loom/target/arch/amdgpu/lower/matrix_fragment_repack.c
index 7d0ccaa37..0fc405ff6 100644
--- a/loom/src/loom/target/arch/amdgpu/lower/matrix_fragment_repack.c
+++ b/loom/src/loom/target/arch/amdgpu/lower/matrix_fragment_repack.c
@@ -125,6 +125,9 @@ iree_string_view_t loom_amdgpu_fragment_repack_plan_key(
       return IREE_SV(
           "amdgpu.fragment_repack.strategy."
           "result_to_rhs_packed_b16_xor_permute");
+    case LOOM_AMDGPU_FRAGMENT_REPACK_STRATEGY_RESULT_F32_TO_RHS_B16_PERMLANE:
+      return IREE_SV(
+          "amdgpu.fragment_repack.strategy.result_f32_to_rhs_b16_permlane");
     case LOOM_AMDGPU_FRAGMENT_REPACK_STRATEGY_DIAGNOSTIC:
       return IREE_SV("amdgpu.fragment_repack.strategy.diagnostic");
     case LOOM_AMDGPU_FRAGMENT_REPACK_STRATEGY_NONE:
@@ -144,6 +147,7 @@ bool loom_amdgpu_fragment_repack_plan_requires_lane_id(
     case LOOM_AMDGPU_FRAGMENT_REPACK_STRATEGY_ALIAS:
     case LOOM_AMDGPU_FRAGMENT_REPACK_STRATEGY_DIAGNOSTIC:
     case LOOM_AMDGPU_FRAGMENT_REPACK_STRATEGY_RESULT_TO_RHS_PACKED_B16_XOR_PERMUTE:
+    case LOOM_AMDGPU_FRAGMENT_REPACK_STRATEGY_RESULT_F32_TO_RHS_B16_PERMLANE:
       return false;
     default:
       IREE_ASSERT_UNREACHABLE("unknown AMDGPU fragment repack strategy");
@@ -665,6 +669,10 @@ static bool loom_amdgpu_fragment_repack_select_target_strategy(
             descriptor_set, layout, plan)) {
       return true;
     }
+    if (loom_amdgpu_select_result_f32_to_rhs_b16_permlane_fragment_repack_plan(
+            descriptor_set, layout, plan)) {
+      return true;
+    }
     if (loom_amdgpu_fragment_repack_select_result_to_lhs_bf16_bpermute_strategy(
             descriptor_set, layout, plan)) {
       return true;
@@ -1650,6 +1658,9 @@ iree_status_t loom_amdgpu_lower_vector_fragment_repack(
     case LOOM_AMDGPU_FRAGMENT_REPACK_STRATEGY_RESULT_TO_RHS_PACKED_B16_XOR_PERMUTE:
       return loom_amdgpu_lower_result_to_rhs_packed_b16_fragment_repack(
           context, source_op, plan);
+    case LOOM_AMDGPU_FRAGMENT_REPACK_STRATEGY_RESULT_F32_TO_RHS_B16_PERMLANE:
+      return loom_amdgpu_lower_result_f32_to_rhs_b16_permlane_fragment_repack(
+          context, source_op, plan);
     case LOOM_AMDGPU_FRAGMENT_REPACK_STRATEGY_DIAGNOSTIC:
       return loom_amdgpu_emit_fragment_repack_diagnostic(context, source_op,
                                                          plan);
diff --git a/loom/src/loom/target/arch/amdgpu/lower/matrix_fragment_repack_packed_b16.c b/loom/src/loom/target/arch/amdgpu/lower/matrix_fragment_repack_packed_b16.c
index 25f6a8439..dcb4ed6a2 100644
--- a/loom/src/loom/target/arch/amdgpu/lower/matrix_fragment_repack_packed_b16.c
+++ b/loom/src/loom/target/arch/amdgpu/lower/matrix_fragment_repack_packed_b16.c
@@ -203,3 +203,188 @@ iree_status_t loom_amdgpu_lower_result_to_rhs_packed_b16_fragment_repack(
                                              result_registers,
                                              plan->result_register_count);
 }
+
+// --- f32 result -> packed-F16 RHS through the half-swap exchange -------------
+//
+// gfx11 wave32 WMMA 16x16x16 with a 32-bit accumulator: result lane l holds
+// column (l mod 16) at rows 2i + (l / 16), i = 0..7. The F16 RHS operand lane l
+// holds column (l mod 16) at k = 0..15, packed two per register (low = even k).
+// Register i of the RHS is therefore (row 2i, row 2i + 1) = (own, partner) in
+// the low half and (partner, own) in the high half, partner = lane ^ 16: one
+// f32 -> f16 convert, one exchange and one byte permute per register, the
+// permute selector chosen once per lane (CK-tile's PermuteWarpGemmCToA).
+static bool loom_amdgpu_fragment_layout_is_gfx11_wave32_acc32(uint32_t kind) {
+  switch (kind) {
+    case LOOM_AMDGPU_MATRIX_FRAGMENT_LAYOUT_RDNA3_WMMAR3_F32_16X16X16_F16:
+    case LOOM_AMDGPU_MATRIX_FRAGMENT_LAYOUT_RDNA3_WMMAR3_F32_16X16X16_BF16:
+    case LOOM_AMDGPU_MATRIX_FRAGMENT_LAYOUT_RDNA3_WMMAR3_I32_16X16X16_IU8:
+    case LOOM_AMDGPU_MATRIX_FRAGMENT_LAYOUT_RDNA3_WMMAR3_I32_16X16X16_IU4:
+      return true;
+    default:
+      return false;
+  }
+}
+
+bool loom_amdgpu_select_result_f32_to_rhs_b16_permlane_fragment_repack_plan(
+    const loom_low_descriptor_set_t* descriptor_set,
+    const loom_amdgpu_matrix_fragment_layout_t* layout,
+    loom_amdgpu_fragment_repack_plan_t* plan) {
+  // An MMA output carries the accumulator role; its lane layout is the result's.
+  if ((plan->source_role != LOOM_CONTRACT_OPERAND_ROLE_RESULT &&
+       plan->source_role != LOOM_CONTRACT_OPERAND_ROLE_ACCUMULATOR) ||
+      plan->result_role != LOOM_CONTRACT_OPERAND_ROLE_RHS ||
+      layout->wave_size != 32 ||
+      !loom_amdgpu_fragment_layout_is_gfx11_wave32_acc32(layout->kind) ||
+      layout->result.register_count != 8 ||
+      layout->result.element_bit_count != 32 ||
+      loom_type_element_type(plan->source_type) != LOOM_SCALAR_TYPE_F32 ||
+      loom_type_element_type(plan->result_type) != LOOM_SCALAR_TYPE_F16) {
+    return false;
+  }
+  static const loom_amdgpu_descriptor_ref_t kRequiredDescriptorRefs[] = {
+      LOOM_AMDGPU_DESCRIPTOR_REF_S_MOV_B32_VCC_IMM,
+      LOOM_AMDGPU_DESCRIPTOR_REF_V_CNDMASK_B32_SRC0_LIT_VCC,
+      LOOM_AMDGPU_DESCRIPTOR_REF_V_MOV_B32,
+      LOOM_AMDGPU_DESCRIPTOR_REF_V_PERM_B32,
+      LOOM_AMDGPU_DESCRIPTOR_REF_V_CVT_F16_F32,
+      LOOM_AMDGPU_DESCRIPTOR_REF_V_PACK_B32_F16,
+  };
+  if (!loom_amdgpu_descriptor_set_has_all_refs(
+          descriptor_set, kRequiredDescriptorRefs,
+          IREE_ARRAYSIZE(kRequiredDescriptorRefs))) {
+    return false;
+  }
+  loom_amdgpu_direct_xor_lane_recipe_t exchange = {0};
+  if (!loom_amdgpu_select_direct_xor_lane_recipe(descriptor_set,
+                                                 /*width=*/32, /*lane_xor=*/16,
+                                                 &exchange)) {
+    return false;
+  }
+  plan->strategy =
+      LOOM_AMDGPU_FRAGMENT_REPACK_STRATEGY_RESULT_F32_TO_RHS_B16_PERMLANE;
+  plan->reason = LOOM_AMDGPU_FRAGMENT_REPACK_REASON_NONE;
+  plan->native_contraction_facts = layout->native_contraction_facts;
+  plan->native_transition_facts = NULL;
+  plan->source_register_count = 8;
+  plan->result_register_count = 8;
+  plan->strategy_payload.result_f32_to_rhs_permlane.exchange = exchange;
+  plan->strategy_payload.result_f32_to_rhs_permlane.pack_ref =
+      LOOM_AMDGPU_DESCRIPTOR_REF_V_CVT_F16_F32;
+  plan->strategy_payload.result_f32_to_rhs_permlane.high_half_lane_mask =
+      0xFFFF0000u;
+  return true;
+}
+
+iree_status_t loom_amdgpu_lower_result_f32_to_rhs_b16_permlane_fragment_repack(
+    loom_low_lower_context_t* context, const loom_op_t* source_op,
+    const loom_amdgpu_fragment_repack_plan_t* plan) {
+  const loom_amdgpu_direct_xor_lane_recipe_t* exchange =
+      &plan->strategy_payload.result_f32_to_rhs_permlane.exchange;
+  loom_value_id_t low_source = LOOM_VALUE_ID_INVALID;
+  IREE_RETURN_IF_ERROR(
+      loom_low_lower_lookup_value(context, plan->source, &low_source));
+  loom_type_t vgpr_type = loom_type_none();
+  IREE_RETURN_IF_ERROR(loom_amdgpu_make_vgpr_type(context, &vgpr_type));
+  loom_type_t vcc_type = loom_type_none();
+  IREE_RETURN_IF_ERROR(loom_amdgpu_make_vcc_type(context, &vcc_type));
+  loom_low_lower_resolved_descriptor_t exchange_descriptor = {0};
+  IREE_RETURN_IF_ERROR(loom_amdgpu_resolve_descriptor_ref(
+      context, exchange->descriptor_ref, &exchange_descriptor));
+  loom_low_lower_resolved_descriptor_t vcc_constant_descriptor = {0};
+  IREE_RETURN_IF_ERROR(loom_amdgpu_resolve_descriptor_ref(
+      context, LOOM_AMDGPU_DESCRIPTOR_REF_S_MOV_B32_VCC_IMM,
+      &vcc_constant_descriptor));
+  loom_low_lower_resolved_descriptor_t selector_descriptor = {0};
+  IREE_RETURN_IF_ERROR(loom_amdgpu_resolve_descriptor_ref(
+      context, LOOM_AMDGPU_DESCRIPTOR_REF_V_CNDMASK_B32_SRC0_LIT_VCC,
+      &selector_descriptor));
+  loom_low_lower_resolved_descriptor_t permute_descriptor = {0};
+  IREE_RETURN_IF_ERROR(loom_amdgpu_resolve_descriptor_ref(
+      context, LOOM_AMDGPU_DESCRIPTOR_REF_V_PERM_B32, &permute_descriptor));
+  loom_low_lower_resolved_descriptor_t convert_descriptor = {0};
+  IREE_RETURN_IF_ERROR(loom_amdgpu_resolve_descriptor_ref(
+      context, plan->strategy_payload.result_f32_to_rhs_permlane.pack_ref,
+      &convert_descriptor));
+  // The per-lane byte selector: (own.low16, partner.low16) in the low half of
+  // the wave, (partner.low16, own.low16) in the high half.
+  loom_value_id_t low_high_half = LOOM_VALUE_ID_INVALID;
+  IREE_RETURN_IF_ERROR(loom_amdgpu_fragment_repack_vcc_constant(
+      context, source_op, &vcc_constant_descriptor,
+      plan->strategy_payload.result_f32_to_rhs_permlane.high_half_lane_mask,
+      vcc_type, &low_high_half));
+  // v_perm_b32 with both sources the packed pair: identity below lane 16,
+  // the halfword swap above.
+  loom_value_id_t low_reversed_selector = LOOM_VALUE_ID_INVALID;
+  IREE_RETURN_IF_ERROR(loom_amdgpu_emit_const_u32(
+      context, source_op, LOOM_AMDGPU_DESCRIPTOR_REF_V_MOV_B32,
+      /*halfword swap*/ 0x01000302u, vgpr_type, &low_reversed_selector));
+  loom_named_attr_t selector_attrs[1];
+  iree_host_size_t selector_attr_count = 0;
+  IREE_RETURN_IF_ERROR(loom_amdgpu_append_i64_attr(
+      context, IREE_SV("imm32"), /*identity*/ 0x03020100u, selector_attrs,
+      IREE_ARRAYSIZE(selector_attrs), &selector_attr_count));
+  const loom_value_id_t selector_operands[] = {low_reversed_selector,
+                                               low_high_half};
+  loom_op_t* low_selector_op = NULL;
+  IREE_RETURN_IF_ERROR(loom_low_lower_emit_resolved_descriptor_op(
+      context, &selector_descriptor, selector_operands,
+      IREE_ARRAYSIZE(selector_operands),
+      loom_make_named_attr_slice(selector_attrs, selector_attr_count),
+      &vgpr_type, 1, /*tied_results=*/NULL, /*tied_result_count=*/0,
+      source_op->location, &low_selector_op));
+  const loom_value_id_t low_selector =
+      loom_value_slice_get(loom_low_op_results(low_selector_op), 0);
+  loom_low_lower_resolved_descriptor_t pack_descriptor = {0};
+  IREE_RETURN_IF_ERROR(loom_amdgpu_resolve_descriptor_ref(
+      context, LOOM_AMDGPU_DESCRIPTOR_REF_V_PACK_B32_F16, &pack_descriptor));
+  // Exchange the f32 registers first (full-register payloads), then convert
+  // own and partner to f16 and pack them; the per-lane byte selector orders
+  // the pair (own, partner) in the low half of the wave, (partner, own) above.
+  loom_value_id_t source_registers[LOOM_AMDGPU_MAX_SCALARIZED_32BIT_LANES] = {
+      LOOM_VALUE_ID_INVALID};
+  loom_value_id_t partner_registers[LOOM_AMDGPU_MAX_SCALARIZED_32BIT_LANES] = {
+      LOOM_VALUE_ID_INVALID};
+  for (uint16_t i = 0; i < plan->source_register_count; ++i) {
+    IREE_RETURN_IF_ERROR(loom_amdgpu_emit_low_slice(
+        context, source_op, low_source, i, vgpr_type, &source_registers[i]));
+  }
+  for (uint16_t i = 0; i < plan->source_register_count; ++i) {
+    IREE_RETURN_IF_ERROR(loom_amdgpu_emit_direct_crosslane_register(
+        context, source_op, &exchange_descriptor, exchange->crosslane_kind,
+        source_registers[i], exchange->immediate, vgpr_type,
+        &partner_registers[i]));
+  }
+  loom_value_id_t result_registers[LOOM_AMDGPU_MAX_SCALARIZED_32BIT_LANES] = {
+      LOOM_VALUE_ID_INVALID};
+  for (uint16_t i = 0; i < plan->source_register_count; ++i) {
+    loom_value_id_t halves[2] = {LOOM_VALUE_ID_INVALID, LOOM_VALUE_ID_INVALID};
+    const loom_value_id_t full[2] = {source_registers[i], partner_registers[i]};
+    for (int h = 0; h < 2; ++h) {
+      loom_op_t* convert_op = NULL;
+      IREE_RETURN_IF_ERROR(loom_low_lower_emit_resolved_descriptor_op(
+          context, &convert_descriptor, &full[h], 1,
+          loom_named_attr_slice_empty(), &vgpr_type, 1, /*tied_results=*/NULL,
+          /*tied_result_count=*/0, source_op->location, &convert_op));
+      halves[h] = loom_value_slice_get(loom_low_op_results(convert_op), 0);
+    }
+    loom_op_t* pack_op = NULL;
+    IREE_RETURN_IF_ERROR(loom_low_lower_emit_resolved_descriptor_op(
+        context, &pack_descriptor, halves, 2, loom_named_attr_slice_empty(),
+        &vgpr_type, 1, /*tied_results=*/NULL, /*tied_result_count=*/0,
+        source_op->location, &pack_op));
+    const loom_value_id_t packed =
+        loom_value_slice_get(loom_low_op_results(pack_op), 0);
+    const loom_value_id_t permute_operands[] = {packed, packed, low_selector};
+    loom_op_t* low_permute_op = NULL;
+    IREE_RETURN_IF_ERROR(loom_low_lower_emit_resolved_descriptor_op(
+        context, &permute_descriptor, permute_operands,
+        IREE_ARRAYSIZE(permute_operands), loom_named_attr_slice_empty(),
+        &vgpr_type, 1, /*tied_results=*/NULL, /*tied_result_count=*/0,
+        source_op->location, &low_permute_op));
+    result_registers[i] =
+        loom_value_slice_get(loom_low_op_results(low_permute_op), 0);
+  }
+  return loom_amdgpu_bind_low_register_range(context, source_op, plan->result,
+                                             result_registers,
+                                             plan->result_register_count);
+}
diff --git a/loom/src/loom/target/arch/amdgpu/lower/matrix_fragment_repack_packed_b16.h b/loom/src/loom/target/arch/amdgpu/lower/matrix_fragment_repack_packed_b16.h
index fdd9a57b0..f747ecd1d 100644
--- a/loom/src/loom/target/arch/amdgpu/lower/matrix_fragment_repack_packed_b16.h
+++ b/loom/src/loom/target/arch/amdgpu/lower/matrix_fragment_repack_packed_b16.h
@@ -28,6 +28,16 @@ iree_status_t loom_amdgpu_lower_result_to_rhs_packed_b16_fragment_repack(
     loom_low_lower_context_t* context, const loom_op_t* source_op,
     const loom_amdgpu_fragment_repack_plan_t* plan);
 
+// f32 result (rows 2i + half per lane) to packed-B16 RHS (k pairs per
+// register): v_permlanex16 half swap, two selects and a pack per register.
+bool loom_amdgpu_select_result_f32_to_rhs_b16_permlane_fragment_repack_plan(
+    const loom_low_descriptor_set_t* descriptor_set,
+    const loom_amdgpu_matrix_fragment_layout_t* layout,
+    loom_amdgpu_fragment_repack_plan_t* plan);
+iree_status_t loom_amdgpu_lower_result_f32_to_rhs_b16_permlane_fragment_repack(
+    loom_low_lower_context_t* context, const loom_op_t* source_op,
+    const loom_amdgpu_fragment_repack_plan_t* plan);
+
 #ifdef __cplusplus
 }  // extern "C"
 #endif
diff --git a/loom/src/loom/target/arch/amdgpu/lower/plan.h b/loom/src/loom/target/arch/amdgpu/lower/plan.h
index 29397570f..a2da685ef 100644
--- a/loom/src/loom/target/arch/amdgpu/lower/plan.h
+++ b/loom/src/loom/target/arch/amdgpu/lower/plan.h
@@ -1000,6 +1000,9 @@ typedef enum loom_amdgpu_crosslane_kind_e {
   LOOM_AMDGPU_CROSSLANE_DPP = 1,
   // Use DS swizzle with a bitmask permutation immediate.
   LOOM_AMDGPU_CROSSLANE_SWIZZLE = 2,
+  // Use v_permlanex16_b32 to exchange the two 16-lane halves of each 32-lane
+  // group (lane xor 16) without touching the LDS crossbar (gfx10+).
+  LOOM_AMDGPU_CROSSLANE_PERMLANEX16 = 3,
 } loom_amdgpu_crosslane_kind_t;
 
 typedef struct loom_amdgpu_direct_xor_lane_recipe_t {
@@ -1648,6 +1651,10 @@ typedef enum loom_amdgpu_fragment_repack_strategy_e {
   LOOM_AMDGPU_FRAGMENT_REPACK_STRATEGY_DIAGNOSTIC = 5,
   // Packed B16 result rows are exchanged and repacked into RHS registers.
   LOOM_AMDGPU_FRAGMENT_REPACK_STRATEGY_RESULT_TO_RHS_PACKED_B16_XOR_PERMUTE = 6,
+  // f32 result rows (2i + half) to packed-B16 RHS k pairs: one half-swap
+  // exchange, two half-dependent selects and one pack per register (gfx11
+  // wave32 WMMA layouts, whose C and B lane layouts differ only by the half).
+  LOOM_AMDGPU_FRAGMENT_REPACK_STRATEGY_RESULT_F32_TO_RHS_B16_PERMLANE = 7,
 } loom_amdgpu_fragment_repack_strategy_t;
 
 typedef enum loom_amdgpu_fragment_repack_reason_e {
@@ -1765,6 +1772,15 @@ typedef struct loom_amdgpu_fragment_repack_plan_t {
       // Wave32 predicate selecting lanes that reverse the packed pair.
       uint32_t reverse_lane_mask;
     } result_to_rhs;
+    // f32 result to packed-B16 RHS through the half-swap exchange.
+    struct {
+      // Cross-lane exchange reading lane ^ 16 (v_permlanex16 when present).
+      loom_amdgpu_direct_xor_lane_recipe_t exchange;
+      // Pack descriptor converting (low, high) f32 to one packed-B16 register.
+      loom_amdgpu_descriptor_ref_t pack_ref;
+      // Wave32 predicate selecting the upper 16 lanes (odd result rows).
+      uint32_t high_half_lane_mask;
+    } result_f32_to_rhs_permlane;
   } strategy_payload;
   // Physical VCC predicates enabling fused conditional transpose stages.
   struct {
diff --git a/loom/src/loom/target/arch/amdgpu/lower/subgroup_crosslane.c b/loom/src/loom/target/arch/amdgpu/lower/subgroup_crosslane.c
index 52ab500ad..22999776d 100644
--- a/loom/src/loom/target/arch/amdgpu/lower/subgroup_crosslane.c
+++ b/loom/src/loom/target/arch/amdgpu/lower/subgroup_crosslane.c
@@ -8,6 +8,7 @@
 
 #include "loom/target/arch/amdgpu/lower/constants.h"
 #include "loom/target/arch/amdgpu/lower/emit.h"
+#include "loom/target/arch/amdgpu/lower/types.h"
 #include "loom/target/arch/amdgpu/lower/subgroup.h"
 #include "loom/target/arch/amdgpu/refs/target_refs.h"
 
@@ -120,6 +121,23 @@ bool loom_amdgpu_select_direct_xor_lane_recipe(
     return true;
   }
 
+  if (lane_xor == 16 && width >= 32 &&
+      loom_amdgpu_descriptor_set_has_ref(
+          descriptor_set,
+          LOOM_AMDGPU_DESCRIPTOR_REF_V_PERMLANEX16_B32_SRC12_SGPR) &&
+      loom_amdgpu_descriptor_set_has_ref(descriptor_set,
+                                         LOOM_AMDGPU_DESCRIPTOR_REF_S_MOV_B32)) {
+    // The half-swap: selectors 0x76543210 / 0xfedcba98 read lane (l ^ 16).
+    *out_recipe = (loom_amdgpu_direct_xor_lane_recipe_t){
+        .descriptor_ref =
+            LOOM_AMDGPU_DESCRIPTOR_REF_V_PERMLANEX16_B32_SRC12_SGPR,
+        .conditional_ref = LOOM_AMDGPU_DESCRIPTOR_REF_NONE,
+        .masked_move_ref = LOOM_AMDGPU_DESCRIPTOR_REF_NONE,
+        .immediate = 0,
+        .crosslane_kind = LOOM_AMDGPU_CROSSLANE_PERMLANEX16,
+    };
+    return true;
+  }
   if (lane_xor < 32 &&
       loom_amdgpu_descriptor_set_has_ref(
           descriptor_set, LOOM_AMDGPU_DESCRIPTOR_REF_DS_SWIZZLE_B32)) {
@@ -214,15 +232,41 @@ iree_status_t loom_amdgpu_emit_direct_crosslane_register(
     loom_value_id_t* out_low_result) {
   *out_low_result = LOOM_VALUE_ID_INVALID;
   IREE_ASSERT(crosslane_kind == LOOM_AMDGPU_CROSSLANE_DPP ||
-              crosslane_kind == LOOM_AMDGPU_CROSSLANE_SWIZZLE);
-  loom_named_attr_t attrs[1];
+              crosslane_kind == LOOM_AMDGPU_CROSSLANE_SWIZZLE ||
+              crosslane_kind == LOOM_AMDGPU_CROSSLANE_PERMLANEX16);
+  loom_named_attr_t attrs[2];
   iree_host_size_t attr_count = 0;
-  const iree_string_view_t immediate_attr_name =
-      crosslane_kind == LOOM_AMDGPU_CROSSLANE_DPP ? IREE_SV("dpp_ctrl")
-                                                  : IREE_SV("offset");
-  IREE_RETURN_IF_ERROR(
-      loom_amdgpu_append_i64_attr(context, immediate_attr_name, immediate,
-                                  attrs, IREE_ARRAYSIZE(attrs), &attr_count));
+  if (crosslane_kind == LOOM_AMDGPU_CROSSLANE_PERMLANEX16) {
+    // v_permlanex16_b32 with identity selectors per half: lane l reads l ^ 16.
+    // The selectors are full 32-bit values, so they travel in SGPRs.
+    loom_type_t sgpr_type = loom_type_none();
+    IREE_RETURN_IF_ERROR(loom_amdgpu_make_sgpr_type(context, &sgpr_type));
+    loom_value_id_t selector_low = LOOM_VALUE_ID_INVALID;
+    IREE_RETURN_IF_ERROR(loom_amdgpu_emit_const_u32(
+        context, source_op, LOOM_AMDGPU_DESCRIPTOR_REF_S_MOV_B32, 0x76543210u,
+        sgpr_type, &selector_low));
+    loom_value_id_t selector_high = LOOM_VALUE_ID_INVALID;
+    IREE_RETURN_IF_ERROR(loom_amdgpu_emit_const_u32(
+        context, source_op, LOOM_AMDGPU_DESCRIPTOR_REF_S_MOV_B32, 0xfedcba98u,
+        sgpr_type, &selector_high));
+    const loom_value_id_t operands[] = {low_source_value, selector_low,
+                                        selector_high};
+    loom_op_t* low_op = NULL;
+    IREE_RETURN_IF_ERROR(loom_low_lower_emit_resolved_descriptor_op(
+        context, descriptor, operands, IREE_ARRAYSIZE(operands),
+        loom_named_attr_slice_empty(), &lane_type, 1,
+        /*tied_results=*/NULL, /*tied_result_count=*/0, source_op->location,
+        &low_op));
+    *out_low_result = loom_value_slice_get(loom_low_op_results(low_op), 0);
+    return iree_ok_status();
+  } else {
+    const iree_string_view_t immediate_attr_name =
+        crosslane_kind == LOOM_AMDGPU_CROSSLANE_DPP ? IREE_SV("dpp_ctrl")
+                                                    : IREE_SV("offset");
+    IREE_RETURN_IF_ERROR(
+        loom_amdgpu_append_i64_attr(context, immediate_attr_name, immediate,
+                                    attrs, IREE_ARRAYSIZE(attrs), &attr_count));
+  }
   loom_op_t* low_op = NULL;
   IREE_RETURN_IF_ERROR(loom_low_lower_emit_resolved_descriptor_op(
       context, descriptor, &low_source_value, 1,
diff --git a/loom/src/loom/target/arch/amdgpu/lower/subgroup_shuffle.c b/loom/src/loom/target/arch/amdgpu/lower/subgroup_shuffle.c
index 35e2ed353..797e52c1a 100644
--- a/loom/src/loom/target/arch/amdgpu/lower/subgroup_shuffle.c
+++ b/loom/src/loom/target/arch/amdgpu/lower/subgroup_shuffle.c
@@ -427,7 +427,8 @@ iree_status_t loom_amdgpu_lower_kernel_subgroup_shuffle(
         &low_source_register));
     switch (plan->crosslane_kind) {
       case LOOM_AMDGPU_CROSSLANE_DPP:
-      case LOOM_AMDGPU_CROSSLANE_SWIZZLE: {
+      case LOOM_AMDGPU_CROSSLANE_SWIZZLE:
+      case LOOM_AMDGPU_CROSSLANE_PERMLANEX16: {
         IREE_RETURN_IF_ERROR(loom_amdgpu_emit_direct_crosslane_register(
             context, source_op, &plan->descriptor, plan->crosslane_kind,
             low_source_register, plan->crosslane_immediate, lane_type,
diff --git a/loom/src/loom/target/arch/amdgpu/test/source_low/source_low_mma_fragment_repack_packed_b16.loom-test b/loom/src/loom/target/arch/amdgpu/test/source_low/source_low_mma_fragment_repack_packed_b16.loom-test
index e61e19789..31bedc1ec 100644
--- a/loom/src/loom/target/arch/amdgpu/test/source_low/source_low_mma_fragment_repack_packed_b16.loom-test
+++ b/loom/src/loom/target/arch/amdgpu/test/source_low/source_low_mma_fragment_repack_packed_b16.loom-test
@@ -29,55 +29,87 @@ low.func.def target<amdgpu.rdna3.core>(@target) abi(hal_kernel) @f16_result_to_r
   %20 = s_mov_b32_vcc_imm 4294901760
   %21 = v_mov_b32 84148480
   %22 = v_cndmask_b32_src0_lit_vcc %21, %20, 16778500
-  %23 = ds_swizzle_b32 %12 {offset = 16415}
-  %24 = ds_swizzle_b32 %13 {offset = 16415}
-  %25 = ds_swizzle_b32 %14 {offset = 16415}
-  %26 = ds_swizzle_b32 %15 {offset = 16415}
-  %27 = ds_swizzle_b32 %16 {offset = 16415}
-  %28 = ds_swizzle_b32 %17 {offset = 16415}
-  %29 = ds_swizzle_b32 %18 {offset = 16415}
-  %30 = ds_swizzle_b32 %19 {offset = 16415}
-  %31 = v_perm_b32 %12, %23, %22
-  %32 = v_perm_b32 %13, %24, %22
-  %33 = v_perm_b32 %14, %25, %22
-  %34 = v_perm_b32 %15, %26, %22
-  %35 = v_perm_b32 %16, %27, %22
-  %36 = v_perm_b32 %17, %28, %22
-  %37 = v_perm_b32 %18, %29, %22
-  %38 = v_perm_b32 %19, %30, %22
-  %rhs = concat(%31, %32, %33, %34, %35, %36, %37, %38) : (reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>) -> reg<amdgpu.vgpr x8>
+  %23 = s_mov_b32 1985229328
+  %24 = s_mov_b32 4275878552
+  %25 = v_permlanex16_b32_src12_sgpr %12, %23, %24
+  %26 = s_mov_b32 1985229328
+  %27 = s_mov_b32 4275878552
+  %28 = v_permlanex16_b32_src12_sgpr %13, %26, %27
+  %29 = s_mov_b32 1985229328
+  %30 = s_mov_b32 4275878552
+  %31 = v_permlanex16_b32_src12_sgpr %14, %29, %30
+  %32 = s_mov_b32 1985229328
+  %33 = s_mov_b32 4275878552
+  %34 = v_permlanex16_b32_src12_sgpr %15, %32, %33
+  %35 = s_mov_b32 1985229328
+  %36 = s_mov_b32 4275878552
+  %37 = v_permlanex16_b32_src12_sgpr %16, %35, %36
+  %38 = s_mov_b32 1985229328
+  %39 = s_mov_b32 4275878552
+  %40 = v_permlanex16_b32_src12_sgpr %17, %38, %39
+  %41 = s_mov_b32 1985229328
+  %42 = s_mov_b32 4275878552
+  %43 = v_permlanex16_b32_src12_sgpr %18, %41, %42
+  %44 = s_mov_b32 1985229328
+  %45 = s_mov_b32 4275878552
+  %46 = v_permlanex16_b32_src12_sgpr %19, %44, %45
+  %47 = v_perm_b32 %12, %25, %22
+  %48 = v_perm_b32 %13, %28, %22
+  %49 = v_perm_b32 %14, %31, %22
+  %50 = v_perm_b32 %15, %34, %22
+  %51 = v_perm_b32 %16, %37, %22
+  %52 = v_perm_b32 %17, %40, %22
+  %53 = v_perm_b32 %18, %43, %22
+  %54 = v_perm_b32 %19, %46, %22
+  %rhs = concat(%47, %48, %49, %50, %51, %52, %53, %54) : (reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>) -> reg<amdgpu.vgpr x8>
   return %rhs
 }
 
 low.func.def target<amdgpu.rdna3.core>(@target) abi(hal_kernel) @bf16_result_to_rhs_packed_b16(%result: reg<amdgpu.vgpr x8>) -> (reg<amdgpu.vgpr x8>) asm {
-  %42 = slice %result[0] : reg<amdgpu.vgpr x8> -> reg<amdgpu.vgpr>
-  %43 = slice %result[1] : reg<amdgpu.vgpr x8> -> reg<amdgpu.vgpr>
-  %44 = slice %result[2] : reg<amdgpu.vgpr x8> -> reg<amdgpu.vgpr>
-  %45 = slice %result[3] : reg<amdgpu.vgpr x8> -> reg<amdgpu.vgpr>
-  %46 = slice %result[4] : reg<amdgpu.vgpr x8> -> reg<amdgpu.vgpr>
-  %47 = slice %result[5] : reg<amdgpu.vgpr x8> -> reg<amdgpu.vgpr>
-  %48 = slice %result[6] : reg<amdgpu.vgpr x8> -> reg<amdgpu.vgpr>
-  %49 = slice %result[7] : reg<amdgpu.vgpr x8> -> reg<amdgpu.vgpr>
-  %50 = s_mov_b32_vcc_imm 4294901760
-  %51 = v_mov_b32 84148480
-  %52 = v_cndmask_b32_src0_lit_vcc %51, %50, 16778500
-  %53 = ds_swizzle_b32 %42 {offset = 16415}
-  %54 = ds_swizzle_b32 %43 {offset = 16415}
-  %55 = ds_swizzle_b32 %44 {offset = 16415}
-  %56 = ds_swizzle_b32 %45 {offset = 16415}
-  %57 = ds_swizzle_b32 %46 {offset = 16415}
-  %58 = ds_swizzle_b32 %47 {offset = 16415}
-  %59 = ds_swizzle_b32 %48 {offset = 16415}
-  %60 = ds_swizzle_b32 %49 {offset = 16415}
-  %61 = v_perm_b32 %42, %53, %52
-  %62 = v_perm_b32 %43, %54, %52
-  %63 = v_perm_b32 %44, %55, %52
-  %64 = v_perm_b32 %45, %56, %52
-  %65 = v_perm_b32 %46, %57, %52
-  %66 = v_perm_b32 %47, %58, %52
-  %67 = v_perm_b32 %48, %59, %52
-  %68 = v_perm_b32 %49, %60, %52
-  %rhs = concat(%61, %62, %63, %64, %65, %66, %67, %68) : (reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>) -> reg<amdgpu.vgpr x8>
+  %58 = slice %result[0] : reg<amdgpu.vgpr x8> -> reg<amdgpu.vgpr>
+  %59 = slice %result[1] : reg<amdgpu.vgpr x8> -> reg<amdgpu.vgpr>
+  %60 = slice %result[2] : reg<amdgpu.vgpr x8> -> reg<amdgpu.vgpr>
+  %61 = slice %result[3] : reg<amdgpu.vgpr x8> -> reg<amdgpu.vgpr>
+  %62 = slice %result[4] : reg<amdgpu.vgpr x8> -> reg<amdgpu.vgpr>
+  %63 = slice %result[5] : reg<amdgpu.vgpr x8> -> reg<amdgpu.vgpr>
+  %64 = slice %result[6] : reg<amdgpu.vgpr x8> -> reg<amdgpu.vgpr>
+  %65 = slice %result[7] : reg<amdgpu.vgpr x8> -> reg<amdgpu.vgpr>
+  %66 = s_mov_b32_vcc_imm 4294901760
+  %67 = v_mov_b32 84148480
+  %68 = v_cndmask_b32_src0_lit_vcc %67, %66, 16778500
+  %69 = s_mov_b32 1985229328
+  %70 = s_mov_b32 4275878552
+  %71 = v_permlanex16_b32_src12_sgpr %58, %69, %70
+  %72 = s_mov_b32 1985229328
+  %73 = s_mov_b32 4275878552
+  %74 = v_permlanex16_b32_src12_sgpr %59, %72, %73
+  %75 = s_mov_b32 1985229328
+  %76 = s_mov_b32 4275878552
+  %77 = v_permlanex16_b32_src12_sgpr %60, %75, %76
+  %78 = s_mov_b32 1985229328
+  %79 = s_mov_b32 4275878552
+  %80 = v_permlanex16_b32_src12_sgpr %61, %78, %79
+  %81 = s_mov_b32 1985229328
+  %82 = s_mov_b32 4275878552
+  %83 = v_permlanex16_b32_src12_sgpr %62, %81, %82
+  %84 = s_mov_b32 1985229328
+  %85 = s_mov_b32 4275878552
+  %86 = v_permlanex16_b32_src12_sgpr %63, %84, %85
+  %87 = s_mov_b32 1985229328
+  %88 = s_mov_b32 4275878552
+  %89 = v_permlanex16_b32_src12_sgpr %64, %87, %88
+  %90 = s_mov_b32 1985229328
+  %91 = s_mov_b32 4275878552
+  %92 = v_permlanex16_b32_src12_sgpr %65, %90, %91
+  %93 = v_perm_b32 %58, %71, %68
+  %94 = v_perm_b32 %59, %74, %68
+  %95 = v_perm_b32 %60, %77, %68
+  %96 = v_perm_b32 %61, %80, %68
+  %97 = v_perm_b32 %62, %83, %68
+  %98 = v_perm_b32 %63, %86, %68
+  %99 = v_perm_b32 %64, %89, %68
+  %100 = v_perm_b32 %65, %92, %68
+  %rhs = concat(%93, %94, %95, %96, %97, %98, %99, %100) : (reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>, reg<amdgpu.vgpr>) -> reg<amdgpu.vgpr x8>
   return %rhs
 }
 
@@ -96,7 +128,7 @@ func.def target(@target) @report_result_to_rhs_packed_b16(%values: vector<16xf16
 
 // ----
 COMPILE-REPORT: summary artifact=none status=OK function=- backend=- bundle=- export=- export_symbol=- config=- lowered=-
-COMPILE-REPORT: source_low selected_ops=2 emitted_ops=28 rows=2
+COMPILE-REPORT: source_low selected_ops=2 emitted_ops=44 rows=2
 COMPILE-REPORT: source_low[0] function=report_result_to_rhs_packed_b16 source_op=index.constant selection=plan emitted_ops=0
-COMPILE-REPORT: source_low[1] function=report_result_to_rhs_packed_b16 source_op=vector.fragment.repack selection=plan plan_key=amdgpu.fragment_repack.strategy.result_to_rhs_packed_b16_xor_permute emitted_ops=28
+COMPILE-REPORT: source_low[1] function=report_result_to_rhs_packed_b16 source_op=vector.fragment.repack selection=plan plan_key=amdgpu.fragment_repack.strategy.result_to_rhs_packed_b16_xor_permute emitted_ops=44
 COMPILE-REPORT: source_low_native[1] function=report_result_to_rhs_packed_b16 source_op=vector.fragment.repack native_contraction={tile:1x16x16x16,participants:32,lhs:{evidence:exact,bits:16,registers:8,payload:16,positions:512,coordinates:256,owners:2},rhs:{evidence:exact,bits:16,registers:8,payload:16,positions:512,coordinates:256,owners:2},accumulator:{evidence:exact,bits:16,registers:8,payload:16,positions:256,coordinates:256,owners:1},result:{evidence:exact,bits:16,registers:8,payload:16,positions:256,coordinates:256,owners:1}} native_transition={result:f16->rhs:f16,positions:512,participant_changes:256,local_position_changes:480,replication:2,source_owner:[participant+=(participant/1%16)*1,participant+=(position/1%2)*16,position+=(position/2)*1]}
diff --git a/loom/src/loom/target/arch/amdgpu/test/source_low/source_low_subgroup_collective.loom-test b/loom/src/loom/target/arch/amdgpu/test/source_low/source_low_subgroup_collective.loom-test
index e6a869140..db8e4726f 100644
--- a/loom/src/loom/target/arch/amdgpu/test/source_low/source_low_subgroup_collective.loom-test
+++ b/loom/src/loom/target/arch/amdgpu/test/source_low/source_low_subgroup_collective.loom-test
@@ -4177,9 +4177,11 @@ low.kernel.def target<amdgpu.gfx11.generic.core>(@target) workgroup_size(64, 1,
   %output_view = resource<hal_binding> {index = 1, source_type = hal.buffer, extent = 256} : reg<amdgpu.sgpr x2>
   %16 = v_lshlrev_b32_src0_inline %tid, 2
   %value = global_load_b32_saddr %16, %input_view
-  %shuffle = ds_swizzle_b32 %value {offset = 16415}
-  %21 = v_lshlrev_b32_src0_inline %tid, 2
-  global_store_b32_saddr %21, %shuffle, %output_view
+  %20 = s_mov_b32 1985229328
+  %21 = s_mov_b32 4275878552
+  %shuffle = v_permlanex16_b32_src12_sgpr %value, %20, %21
+  %23 = v_lshlrev_b32_src0_inline %tid, 2
+  global_store_b32_saddr %23, %shuffle, %output_view
   return
 }
 
-- 
2.55.0

